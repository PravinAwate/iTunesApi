# iTunesApi

Clone the repo

git clone git@github.com:rsbh/express-typescript.git

Start server

cd iTunes-Server
Install dependencies.

yarn install
Build the  server.

yarn build
Run the server.

yarn serve

Start UI
cd itunes-ui
Install dependencies.

yarn install
Build the  server.

yarn build
Run the server.

yarn serve


